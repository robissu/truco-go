services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: truco
      POSTGRES_PASSWORD: truco
      POSTGRES_DB: truco
    ports:
      - "5432:5432"
    volumes:
      - ./migrations:/docker-entrypoint-initdb.d:ro
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: